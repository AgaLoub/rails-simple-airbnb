<h1>Nos apparts à Paris</h1>

<% @flats.each do |flat| %>
  <p><%= link_to "#{flat.name}", flat_path(flat) %>
  <%= link_to ('<i class="fa fa-trash" aria-hidden="true"></i>').html_safe, flat_path(flat), method: :delete,
      data: { confirm: "Are you sure?" } %></p>
  <% end %>

<%= link_to "Add a new flat", new_flat_path %>



